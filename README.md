# SkillBridge Frontend

Frontend-only React + Vite project. Uses demo data from the uploaded NLP job dataset. No backend, database, API, or NLP algorithm is included.

Run in VS Code terminal:

npm install
npm run dev

Then open the localhost URL shown by Vite.
