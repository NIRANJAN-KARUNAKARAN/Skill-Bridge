import { NavLink, Link, useNavigate } from 'react-router-dom';
const nav = [['⌂','Dashboard','/dashboard'],['⌕','Jobs','/jobs'],['◈','Skills','/skills'],['◫','Skill Gap','/skill-gap'],['✦','Recommendations','/recommendations'],['▥','Market Insights','/insights'],['♢','Notifications','/notifications'],['◉','Profile','/profile']];
export const Logo = () => <Link to="/" className="logo"><span>⌁</span> SkillBridge</Link>;
export function AppLayout({ title, subtitle, children, action }) { const navigate = useNavigate(); return <div className="app-shell"><aside className="sidebar"><Logo/><nav>{nav.map(([icon,label,to]) => <NavLink key={to} to={to}><i>{icon}</i>{label}</NavLink>)}</nav><div className="side-footer"><Link to="/about">About SkillBridge</Link><button onClick={()=>navigate('/')}>Sign out</button></div></aside><main><header className="app-header"><div><p className="eyebrow">STUDENT WORKSPACE</p><h1>{title}</h1>{subtitle&&<p className="subtle">{subtitle}</p>}</div><div className="header-actions">{action}<Link to="/notifications" className="icon-button" aria-label="Notifications">♢</Link><Link to="/profile" className="avatar">SB</Link></div></header><div className="page-content">{children}</div></main></div> }
export function Status({ children, type='neutral' }) { return <span className={`status ${type}`}>{children}</span> }
export function EmptyState({ title, text, action }) { return <div className="empty"><div>⌕</div><h3>{title}</h3><p>{text}</p>{action}</div> }
